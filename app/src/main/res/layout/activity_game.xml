<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layout_gravity="center"
    android:padding="16dp">

    <!-- Input field for user to type their 5-digit guess -->
    <EditText
        android:id="@+id/guessInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter your 5-digit guess"
        android:inputType="number"
        android:maxLength="5"
        android:gravity="center"
        android:textSize="40sp"
        android:layout_marginBottom="16dp" />

    <!-- Button to submit guess -->
    <Button
        android:id="@+id/submitGuessButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Submit Guess"
        android:layout_gravity="center"
        android:textSize="40sp"
        android:layout_marginBottom="16dp" />

    <!-- Button to get a hint about duplicate digits -->
    <Button
        android:id="@+id/hintButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Get Hint"
        android:layout_gravity="center"
        android:textSize="30sp"
        android:layout_marginBottom="16dp" />

    <!-- GridLayout to show the guesses -->
    <GridLayout
        android:id="@+id/guessGrid"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:columnCount="5"
        android:rowCount="5"
        android:layout_gravity="center"
        android:layout_marginTop="16dp"
        android:padding="8dp"
        android:background="#E0E0E0">
    </GridLayout>

</LinearLayout>
